/*Java döngüler ile tek bir sayı girilene kadar
 kullanıcıdan girişleri kabul eden ve girilen değerlerden
 çift ve 4'ün katları olan sayıları toplayıp ekrana basan programı yazıyoruz.
 */

import java.util.Scanner;
public class checkEven4 {
    public static void main(String[] args) {
        int number, sum=0;
        Scanner inp=new Scanner(System.in);
        do{
            System.out.print("Enter a number:");
            number= inp.nextInt();
            if(number%4==0){
                sum+=number;
            }
        }while(number%2==0);
        System.out.println("the sum of the even and multiple of 4 of entered numbers:"+sum);
    }
}
